# Credentials configuration for Rasa
# This file contains the credentials for the different channels your bot can use.

# REST channel credentials
rest:
  # Nothing needed here

# Facebook Messenger credentials
# facebook:
#   verify: "your_verify_token"
#   secret: "your_secret"
#   page-access-token: "your_page_access_token"

# Slack credentials
# slack:
#   slack_token: "your_slack_token"
#   slack_channel: "#your_channel"

# Telegram credentials
# telegram:
#   access_token: "your_telegram_token"
#   verify: "your_bot_name"
#   webhook_url: "https://your-domain.com/webhooks/telegram/webhook"

# WhatsApp Business API credentials (Meta - Direct Integration)
whatsapp:
  verify_token: "${WHATSAPP_VERIFY_TOKEN}"
  access_token: "${WHATSAPP_ACCESS_TOKEN}"
  phone_number_id: "${WHATSAPP_PHONE_NUMBER_ID}"
  business_account_id: "${WHATSAPP_BUSINESS_ACCOUNT_ID}"
  webhook_url: "${WHATSAPP_WEBHOOK_URL}"

# Socket.IO credentials
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

